# Pipeline de QA — Ejercicio 4
#
# Corre los ejercicios 1, 2 y 3 en cada push y cada pull request, y publica un
# reporte consolidado.
#
# Sobre el orden de los jobs: el enunciado pide Web -> API -> Chatbot/LLM y eso
# es lo que se respeta. Técnicamente convendría el inverso (la API es la más
# rápida y barata, así que fallar ahí primero ahorra minutos de runner), pero en
# una prueba técnica se sigue la especificación y se documenta la observación.
#
# Los tres jobs de pruebas llevan `continue-on-error` a propósito: sin eso, el
# primer fallo aborta el pipeline y el reporte consolidado nunca se genera —
# justo cuando más se necesita. Aquí siempre se produce, y el job final es el
# que decide si el pipeline pasa o no.

name: QA — Prueba Técnica

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:

env:
  API_BASE_URL: https://goal-tracker-api.onrender.com
  CHATBOT_URL: https://botpress.com/docs

jobs:
  # ---------------------------------------------------------------- 1. Web
  web:
    name: Ejercicio 3 — Chatbot Web
    runs-on: ubuntu-latest
    continue-on-error: true
    defaults:
      run:
        working-directory: ejercicio-2-3-playwright
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: ejercicio-2-3-playwright/package-lock.json

      - name: Instalar dependencias
        run: npm ci

      - name: Instalar navegador
        run: npx playwright install --with-deps chromium

      - name: Pruebas de interfaz
        run: npm run test:ui

      - name: Guardar resultados
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: resultados-web
          path: output/
          retention-days: 14

  # ---------------------------------------------------------------- 2. API
  api:
    name: Ejercicio 2 — API
    runs-on: ubuntu-latest
    continue-on-error: true
    defaults:
      run:
        working-directory: ejercicio-2-3-playwright
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: ejercicio-2-3-playwright/package-lock.json

      - name: Instalar dependencias
        run: npm ci

      - name: Despertar la API
        # Render duerme el contenedor en el plan gratuito. Se le da un empujón
        # antes de empezar para que el arranque en frío no cuente como fallo.
        run: curl -s --max-time 120 "$API_BASE_URL/api/v1/status" || true

      - name: Pruebas de API
        run: npm run test:api

      - name: Guardar resultados
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: resultados-api
          path: output/
          retention-days: 14

  # ------------------------------------------------------------ 3. LLM
  llm:
    name: Ejercicio 1 — Evaluación LLM
    runs-on: ubuntu-latest
    continue-on-error: true
    defaults:
      run:
        working-directory: ejercicio-1-llm-eval
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: ejercicio-1-llm-eval/requirements.txt

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Pruebas del propio evaluador
        # Corren sin credenciales y sin red. Si el evaluador está roto, no tiene
        # sentido gastar llamadas al modelo: se corta aquí.
        run: python -m pytest tests/ -v

      - name: Elegir proveedor según haya credenciales
        id: proveedor
        env:
          CLAVE: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          if [ -n "$CLAVE" ]; then
            echo "valor=anthropic" >> "$GITHUB_OUTPUT"
            echo "Corriendo contra el modelo real."
          else
            echo "valor=mock" >> "$GITHUB_OUTPUT"
            echo "Sin ANTHROPIC_API_KEY: se corre en modo mock con fixtures."
          fi

      - name: Evaluación conversacional
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          LLM_MODEL: claude-haiku-4-5
          JUDGE_MODEL: claude-sonnet-5
        run: |
          python src/runner.py --todos \
            --proveedor "${{ steps.proveedor.outputs.valor }}" \
            --fixture fixtures/asistente-sano.yaml

      - name: Verificar que el evaluador detecta fallas plantadas
        # Contraprueba: con el fixture defectuoso los 5 escenarios TIENEN que
        # fallar. Si pasan, el evaluador dejó de detectar y el pipeline debe
        # enterarse.
        run: |
          if python src/runner.py --todos --proveedor mock \
               --fixture fixtures/asistente-defectuoso.yaml > /dev/null 2>&1; then
            echo "::error::El evaluador NO detectó las fallas plantadas."
            exit 1
          fi
          echo "El evaluador detectó correctamente las fallas plantadas."

      - name: Guardar resultados
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: resultados-llm
          path: output/
          retention-days: 14

  # ------------------------------------------------------- 4. Consolidación
  consolidar:
    name: Ejercicio 5 — Reporte consolidado
    runs-on: ubuntu-latest
    needs: [web, api, llm]
    if: always()
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Recuperar los resultados de los tres ejercicios
        uses: actions/download-artifact@v4
        with:
          path: artefactos

      - name: Unificar en output/
        run: |
          mkdir -p output
          for dir in artefactos/*/; do
            [ -d "$dir" ] && cp -R "$dir." output/ || true
          done
          find output -type f | sort

      - name: Generar reporte consolidado
        run: python ejercicio-5-reporte/consolidar.py --entrada output --salida output

      - name: Publicar el reporte en el resumen del job
        if: always()
        run: cat output/reporte-consolidado.md >> "$GITHUB_STEP_SUMMARY"

      - name: Guardar reporte
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: reporte-consolidado
          path: output/
          retention-days: 30

      - name: Decidir el resultado del pipeline
        # Los jobs de pruebas no cortan el pipeline (continue-on-error) para que
        # el reporte siempre se genere. La decisión final se toma aquí, con el
        # consolidado ya en la mano.
        run: python ejercicio-5-reporte/consolidar.py --entrada output --salida output --estricto
